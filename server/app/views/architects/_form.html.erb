<%= form_with(model: architect, local: true) do |form| %>
  <% if architect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(architect.errors.count, "error") %> prohibited this architect from being saved:</h2>

      <ul>
        <% architect.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :position %>
    <%= form.collection_select :position_id, ArchitectPosition.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :desc %>
    <%= form.text_area :desc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
